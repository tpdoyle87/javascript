// cannot define a variable more then once
let petName = 'Hal';

petName = 'Spot';

console.log(petName);

// there are rules to variable names

let  3 = 2; //invalid
let result = 3 + 4;

// variable names cannot be reserved keywords
// ex: let, const, function, etc
